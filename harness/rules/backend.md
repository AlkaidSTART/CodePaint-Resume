# Backend Rules

- API 使用版本化路径和统一错误结构，输入在边界处校验。
- 认证、角色、workspace 和资源 owner 必须在服务端校验。
- 列表接口统一分页、排序和筛选上限，避免无界查询。
- 异步解析任务必须幂等、可重试、可观测，并保留原始文件和解析版本。
- 文件下载使用短时签名 URL 或受保护代理，不暴露永久公开地址。
- 邮箱凭据、Token 和候选人敏感信息禁止进入日志。

