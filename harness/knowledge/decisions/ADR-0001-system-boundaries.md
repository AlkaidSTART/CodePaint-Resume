# ADR-0001 两套 UI 与统一 API

## 状态

Accepted

## 决策

采用两个独立 React 应用：`public-web` 面向普通用户，`admin-web` 面向招新成员；二者通过统一 Go API 访问业务能力，并共享类型、API client、认证工具和基础 UI。

## 原因

两端的信息密度、导航结构、权限边界和迭代节奏不同。分开应用可减少误展示内部能力的风险，同时保留共享契约带来的开发一致性。

## 后果

需要维护两套构建和部署入口；共享包必须避免混入业务页面和角色专属状态。

