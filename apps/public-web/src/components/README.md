# Public web components

页面级编排放在 `src/routes/`，页面业务组件按职责归类，避免所有组件堆在同一目录：

- `layout/`：公开端的全局页面壳、导航和全局覆盖层挂载。
- `home/`：首页编排和首页各个内容区块，保持公开端品牌内容与后台业务隔离。
- `application/`：报名表单及报名流程相关覆盖层。

跨应用、无业务倾向的基础组件应放在 `packages/ui`，不放入这里。

首页的页面编排器位于 `src/routes/home/`，只负责组合首页区块与滚动时序。
