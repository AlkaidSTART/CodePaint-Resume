# Backend Database Rules

- Schema 变更通过版本化迁移完成，不依赖生产数据库手工状态；
- 外键、唯一约束和状态约束优先由数据库保证，应用层再提供友好错误；
- 依据实际查询增加索引，避免无证据添加索引和 JSONB 无界扫描；
- 候选人和报名查询必须带 workspace/owner 条件；
- 原始文件引用、解析版本和审计记录按生命周期保留，禁止静默删除；
- 迁移需考虑旧版本并发运行、回滚和大表锁风险。

