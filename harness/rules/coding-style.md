# Coding Style Rules

- 命名表达业务含义，避免无上下文的 `data`、`info` 和 `handleThing`；
- 文件和模块按职责组织，避免一个文件同时承载协议、业务、存储和展示；
- 注释解释原因、约束和非显然取舍，不重复代码表面行为；
- 日志包含可检索事件名和关联 id，默认脱敏；
- 优先小而清晰的函数和显式依赖，不引入没有使用场景的抽象；
- 格式化和 lint 使用仓库配置，不在单次任务中引入风格迁移。

