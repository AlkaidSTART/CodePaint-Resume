# Error Handling Rules

- 输入错误返回字段级或参数级可行动提示；
- 认证/授权错误不泄露资源是否存在的额外信息；
- 业务冲突返回稳定错误码，前端可据此刷新或提示；
- 外部依赖错误按策略重试或进入失败态，并保留上下文；
- 未知错误记录 trace id，向用户返回通用提示；
- 重试必须有次数、退避和幂等保证；
- UI 必须提供重试、返回或联系支持等明确路径。

