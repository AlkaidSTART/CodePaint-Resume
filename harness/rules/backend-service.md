# Backend Service Rules

- Handler 只做协议适配，业务规则放在 service，持久化放在 repository；
- 涉及多个写操作时明确事务边界和失败回滚行为；
- 状态机只允许显式定义的状态转换，非法转换返回稳定错误；
- 外部 Provider 调用必须有超时、重试上限、幂等策略和失败记录；
- 异步任务 payload 使用稳定 id，不复制大文件或敏感原文；
- 审计记录包含操作者、动作、资源、时间和结果，但不写入敏感内容。

